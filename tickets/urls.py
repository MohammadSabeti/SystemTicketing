
# from django.contrib import admin
from django.urls import path
from .views import *

app_name='tickets'
urlpatterns = [
       # path("ticket", ticket_list_view, name="ticket_list"),
       # path("ticket/<int:id>", ticket_detail_view, name="ticket_detail"),
       # path("ticket/create", ticket_form_view, name="ticket_form"),
       # path("ticket/<int:id>", ticket_edite_view, name="ticket_edite"),
       # path("ticket/<int:id>", ticket_delete_view, name="ticket_delete"),
]
